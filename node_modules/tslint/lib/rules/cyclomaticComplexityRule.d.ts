import * as Lint from "../lint";
import * as ts from "typescript";
export declare class Rule extends Lint.Rules.AbstractRule {
    static DEFAULT_THRESHOLD: number;
    static MINIMUM_THRESHOLD: number;
    static metadata: Lint.IRuleMetadata;
    static ANONYMOUS_FAILURE_STRING: (expected: number, actual: number) => string;
    static NAMED_FAILURE_STRING: (expected: number, actual: number, name: string) => string;
    apply(sourceFile: ts.SourceFile): Lint.RuleFailure[];
    isEnabled(): boolean;
    private readonly threshold;
}
